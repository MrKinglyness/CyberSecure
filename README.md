CyberSecure student project

What this is
A full stack web app that lets a logged in user store a history of security scan records
It includes a front end interface a database and a REST api with full crud

Demo login
Email demo@cybersecure.local
Password demo1234

Pages
Login
Dashboard list scans
Create scan
View scan
Edit scan
Delete scan

Api endpoints
GET /api/scans
POST /api/scans
GET /api/scans/<id>
PUT /api/scans/<id>
DELETE /api/scans/<id>
